'use strict'


import * as todo from './todo/types'
import * as users from './users/types'
import * as topic from './topic/types'
import * as filter from './filter/types'


const types = {
  todo,
  users,
  topic,
  filter
}

export default types
